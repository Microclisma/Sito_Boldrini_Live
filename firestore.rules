rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /articles/{document=**} {
      // Consente la lettura a tutti
      allow read: if true;
      // Consente la scrittura solo agli utenti autenticati (admin)
      allow write: if request.auth != null;
    }
  }
}
